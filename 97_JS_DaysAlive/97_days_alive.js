// Ask user input for age
var age = prompt("What is your age?")

// Calculate days alive
var days_alive = age * 365.25

// Give user the number of days alive
alert(days_alive + " days have passed since you were born!")